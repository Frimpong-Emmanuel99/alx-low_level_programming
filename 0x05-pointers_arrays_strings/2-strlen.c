#include "main.h"

/**
 * _strlen - Entry point
 * @s: The string to be checked
 * Return: the length
 */


int _strlen(char *s)
{
	int length;

	for (length = 0; s[length] != '\0'; length++)
	{

	}
	return (length);
}
