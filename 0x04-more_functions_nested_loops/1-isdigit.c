#include "main.h"


/**
 * _isdigit - A program to check if a value is a digit
 * @c: the character checker
 * Return: return 1 if digit or 0 if otherwise
 */


int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
